/* import GET_DATA from "../actions/index"; */

const dataArray = {
  content: [],
};

const dataReducer = (state = dataArray, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default dataReducer;
